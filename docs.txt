DOCUMENTATION

FOR BUILDING:
Use "Make SFX
	use script.bat under "Extract files into specified folder and launch executable file"
	Extraction folder should be TEMP
	Run as Admin under UAC auto elev
	Auto overwrite all files under Overwrite Mode
	Do not delete extracted files under How to delete
	Working directory should be the default Extraction Folder

TOTAL STEPS:
With the above steps, we've built Fasternet.exe in this folder
1) Copy it over to the pythonbundled folder and follow doc.txt in there
2) Copy the output from pythonbundled into Inno2 and follow doc.txt in there
